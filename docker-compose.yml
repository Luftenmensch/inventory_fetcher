version: "3.8"
services:
  server:
    build: server/
    ports:
      - "8000:8000"
    networks:
      - mynetwork
  client:
    build: client/
    networks:
      - mynetwork
  redis:
    image: redis
    ports:
      - "6379:6379"
    networks:
      - mynetwork
networks:
  mynetwork: